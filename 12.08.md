前端（H5 网页）部署

需要上传的文件

apps/wx-app/dist/build/h5/ # 整个构建产物目录

部署步骤

1. 本地构建
   pnpm build:wx

2. 上传到服务器

# 使用 scp 上传（假设服务器地址为 your-server.com）

scp -r apps/wx-app/dist/build/h5/\* user@your-server.com:/var/www/html/yunshi/

# 或使用 rsync（推荐，支持增量同步）

rsync -avz --delete apps/wx-app/dist/build/h5/ user@your-server.com:/var/www/html/yunshi/

3.  Nginx 配置示例
    server {
    listen 80;
    server_name yunshi.autopia.chat;

        root /var/www/html/yunshi;
        index index.html;

        # H5 单页应用路由支持
        location / {
            try_files $uri $uri/ /index.html;
        }

        # API 反向代理（如果前后端同域）
        location /api/ {
            proxy_pass http://localhost:3000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # AI 生成接口超时设置
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
            proxy_send_timeout 120s;
        }

        # Gzip 压缩
        gzip on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml;

    }

---

后端（NestJS API）部署

需要上传的文件

apps/api/dist/ # 构建产物（必需）
apps/api/prisma/ # Prisma Schema 和迁移（必需）
apps/api/package.json # 依赖声明（必需）
apps/api/.env.production # 生产环境变量（必需）
apps/api/node_modules/ # 不推荐上传，在服务器安装

部署步骤

1. 本地构建

# 构建后端代码

pnpm build:api

2. 准备生产环境配置

# 复制环境变量模板

cp deployment/.env.production.template apps/api/.env.production

# 编辑 .env.production，配置生产环境参数

nano apps/api/.env.production

.env.production 关键配置：
NODE_ENV=production
PORT=3000

# 数据库（使用远程 PostgreSQL）

DATABASE_URL="postgresql://user:password@db-host:5432/yunshi?schema=public"

# JWT 密钥（必须修改！）

JWT_SECRET=your-super-secret-key-change-in-production

# 微信配置

WECHAT_APP_ID=your-app-id
WECHAT_APP_SECRET=your-app-secret

# AI 服务（火山引擎 Doubao）

OPENAI_API_KEY=your-doubao-api-key
OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3

3. 上传到服务器

# 创建服务器目录

ssh user@your-server.com "mkdir -p /opt/yunshi-api"

# 上传构建产物和必要文件

scp -r apps/api/dist user@your-server.com:/opt/yunshi-api/
scp -r apps/api/prisma user@your-server.com:/opt/yunshi-api/
scp apps/api/package.json user@your-server.com:/opt/yunshi-api/
scp apps/api/.env.production user@your-server.com:/opt/yunshi-api/.env

4. 服务器端安装依赖

# SSH 登录服务器

ssh user@your-server.com

# 进入项目目录

cd /opt/yunshi-api

# 安装生产依赖（只安装 dependencies，不装 devDependencies）

npm install --production

# 或使用 pnpm（需先安装 pnpm）

npm install -g pnpm
pnpm install --prod

5. 数据库迁移

# 在服务器上执行

cd /opt/yunshi-api

# 安装 Prisma CLI（如果使用 --production 会缺失）

npm install prisma

# 应用数据库迁移

npx prisma migrate deploy

# 生成 Prisma Client

npx prisma generate

# 可选：填充种子数据（首次部署）

node dist/prisma/seed.js

6. 启动服务

方式一：直接启动（不推荐，终端关闭后进程会停止）
NODE_ENV=production node dist/main.js

方式二：使用 PM2（推荐）

# 安装 PM2

npm install -g pm2

# 启动应用

pm2 start dist/main.js --name yunshi-api --env production

# 设置开机自启

pm2 startup
pm2 save

# 常用 PM2 命令

pm2 list # 查看进程列表
pm2 logs yunshi-api # 查看日志
pm2 restart yunshi-api # 重启
pm2 stop yunshi-api # 停止
pm2 delete yunshi-api # 删除进程

方式三：使用 systemd 服务

创建 /etc/systemd/system/yunshi-api.service：
[Unit]
Description=Yunshi API Service
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/yunshi-api
Environment=NODE_ENV=production
ExecStart=/usr/bin/node /opt/yunshi-api/dist/main.js
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target

启动服务：
sudo systemctl daemon-reload
sudo systemctl enable yunshi-api
sudo systemctl start yunshi-api

# 查看状态

sudo systemctl status yunshi-api

# 查看日志

sudo journalctl -u yunshi-api -f

---

完整部署脚本示例

● 本地构建并上传脚本 deploy.sh：

#!/bin/bash
set -e

SERVER="user@your-server.com"
FRONTEND_DIR="/var/www/html/yunshi"
BACKEND_DIR="/opt/yunshi-api"

echo "=== 开始部署 ==="

# 1. 本地构建

echo "📦 构建前端..."
pnpm build:wx

echo "📦 构建后端..."
pnpm build:api

# 2. 上传前端

echo "🚀 上传前端到 $FRONTEND_DIR..."
  rsync -avz --delete apps/wx-app/dist/build/h5/ $SERVER:$FRONTEND_DIR/

# 3. 上传后端

echo "🚀 上传后端到 $BACKEND_DIR..."
  ssh $SERVER "mkdir -p $BACKEND_DIR"
  rsync -avz --delete apps/api/dist/ $SERVER:$BACKEND_DIR/dist/
rsync -avz apps/api/prisma/ $SERVER:$BACKEND_DIR/prisma/
scp apps/api/package.json $SERVER:$BACKEND_DIR/
scp apps/api/.env.production $SERVER:$BACKEND_DIR/.env

# 4. 服务器端操作

echo "⚙️ 服务器端安装依赖和迁移数据库..."
ssh $SERVER << 'EOF'
cd /opt/yunshi-api
npm install --production
npx prisma migrate deploy
npx prisma generate
pm2 restart yunshi-api || pm2 start dist/main.js --name yunshi-api
EOF

echo "✅ 部署完成！"
echo "前端地址: https://yunshi.autopia.chat"
echo "后端地址: https://yunshi.autopia.chat/api"

使用方法：
chmod +x deploy.sh
./deploy.sh

---

注意事项

1. 数据库：生产环境使用远程 PostgreSQL，不要用 Docker Compose 的本地数据库
2. 环境变量：.env.production 不要提交到 Git，包含敏感信息
3. JWT 密钥：生产环境必须修改 JWT_SECRET
4. CDN 超时：如使用腾讯云 EdgeOne，需将回源超时设置为 ≥120 秒
5. 日志监控：使用 PM2 或 systemd 可方便查看日志
6. HTTPS：生产环境建议配置 SSL 证书（Let's Encrypt）
7. 防火墙：确保服务器开放 80/443（Nginx）和 3000 端口（API，如果直接暴露）
